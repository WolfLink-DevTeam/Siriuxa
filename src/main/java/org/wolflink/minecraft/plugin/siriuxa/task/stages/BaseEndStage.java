package org.wolflink.minecraft.plugin.siriuxa.task.stages;

public abstract class BaseEndStage extends TaskStage {
    public BaseEndStage(TaskLinearStageHolder stageHolder) {
        super("已结束", stageHolder);
    }
}
